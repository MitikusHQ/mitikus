# Mi Office Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agrupar Docs, Hojas de cálculo y PDFs bajo un único ítem "Mi Office" en el sidebar con página hub propia.

**Architecture:** Cambios puramente de navegación y UI. Se añade un icono `office` a `WorkspaceIcons`, se crea una página hub Server Component en `/office`, se actualiza `layout.tsx` para sustituir 3 ítems por uno, y se elimina el bloque "Herramientas de Office" de `/tools`. Sin cambios de rutas ni lógica de datos.

**Tech Stack:** Next.js 15 App Router, TypeScript, Tailwind CSS.

---

## Archivos

| Acción | Archivo |
|--------|---------|
| Modificar | `apps/web/src/app/(dashboard)/workspace/[workspaceId]/_components/WorkspaceIcons.tsx` |
| Crear | `apps/web/src/app/(dashboard)/workspace/[workspaceId]/office/page.tsx` |
| Modificar | `apps/web/src/app/(dashboard)/workspace/[workspaceId]/layout.tsx` |
| Modificar | `apps/web/src/app/(dashboard)/workspace/[workspaceId]/tools/page.tsx` |

---

### Task 1: Añadir icono `office` a WorkspaceIcons + crear página hub `/office`

**Files:**
- Modify: `apps/web/src/app/(dashboard)/workspace/[workspaceId]/_components/WorkspaceIcons.tsx`
- Create: `apps/web/src/app/(dashboard)/workspace/[workspaceId]/office/page.tsx`

- [ ] **Step 1: Añadir `Icons.office` en WorkspaceIcons.tsx**

Localiza el cierre del objeto `Icons` (antes del `}` final) y añade la entrada `office`:

```tsx
  office: (
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden>
      <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/>
    </svg>
  ),
```

- [ ] **Step 2: Crear la página hub `/office/page.tsx`**

```tsx
// apps/web/src/app/(dashboard)/workspace/[workspaceId]/office/page.tsx
import { requireUser } from '@/lib/auth'
import Link from 'next/link'

interface Props {
  params: Promise<{ workspaceId: string }>
}

const TOOLS = [
  {
    href:     (base: string) => `${base}/docs`,
    emoji:    '📄',
    title:    'Documentos',
    subtitle: 'Base de conocimiento del workspace',
  },
  {
    href:     (base: string) => `${base}/sheets`,
    emoji:    '📊',
    title:    'Hojas de cálculo',
    subtitle: 'Datos, presupuestos y análisis',
  },
  {
    href:     (base: string) => `${base}/pdfs`,
    emoji:    '📑',
    title:    'PDFs',
    subtitle: 'Visor, búsqueda y conversión a Doc',
  },
] as const

export default async function MyOfficePage({ params }: Props) {
  const [{ workspaceId }] = await Promise.all([params, requireUser()])
  const base = `/workspace/${workspaceId}`

  return (
    <div className="max-w-3xl mx-auto px-6 py-8 space-y-6">
      <div>
        <h1 className="text-xl font-semibold">Mi Office</h1>
        <p className="text-xs text-muted-foreground mt-0.5">
          Tus herramientas de documento
        </p>
      </div>

      <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
        {TOOLS.map((tool) => (
          <Link
            key={tool.title}
            href={tool.href(base)}
            className="flex flex-col gap-3 rounded-lg border p-5 hover:border-primary/50 hover:bg-muted/50 transition-colors"
          >
            <span className="text-3xl">{tool.emoji}</span>
            <div>
              <p className="text-sm font-medium">{tool.title}</p>
              <p className="text-xs text-muted-foreground mt-0.5">{tool.subtitle}</p>
            </div>
            <span className="text-xs text-primary font-medium">BUILT-IN</span>
          </Link>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: TypeScript check**

```powershell
cd C:\Users\priet\protools-hub\apps\web
npx tsc --noEmit 2>&1 | Select-String "office|WorkspaceIcons" | Select-Object -First 10
```

Expected: sin output (sin errores).

- [ ] **Step 4: Commit**

```powershell
cd C:\Users\priet\protools-hub
git add "apps/web/src/app/(dashboard)/workspace/[workspaceId]/_components/WorkspaceIcons.tsx"
git add "apps/web/src/app/(dashboard)/workspace/[workspaceId]/office/page.tsx"
git commit -m "feat: add Mi Office hub page and office icon"
```

---

### Task 2: Actualizar `layout.tsx` y `tools/page.tsx`

**Files:**
- Modify: `apps/web/src/app/(dashboard)/workspace/[workspaceId]/layout.tsx`
- Modify: `apps/web/src/app/(dashboard)/workspace/[workspaceId]/tools/page.tsx`

Lee ambos archivos antes de modificar.

- [ ] **Step 1: Actualizar `layout.tsx` — sustituir 3 ítems por Mi Office**

En `mainItems`, localiza y **elimina** estos tres objetos:

```typescript
// ELIMINAR:
{
  label: 'Docs',
  href: `${base}/docs`,
  icon: Icons.docs,
  description: 'Base de conocimiento del workspace',
},
{
  label: 'Hojas de cálculo',
  href: `${base}/sheets`,
  icon: Icons.sheets,
  description: 'Hojas de cálculo con fórmulas y datos',
},
{
  label: 'PDFs',
  href:  `${base}/pdfs`,
  icon:  Icons.pdf,
  description: 'Documentos PDF del workspace',
},
```

Y **añade** en su lugar (después del ítem `Clientes`):

```typescript
{
  label: 'Mi Office',
  href: `${base}/office`,
  icon: Icons.office,
  description: 'Documentos, hojas de cálculo y PDFs del workspace',
},
```

- [ ] **Step 2: Actualizar `tools/page.tsx` — eliminar bloque "Herramientas de Office"**

Localiza y elimina el bloque completo (incluye el comentario, el `<div className="space-y-3">` y su contenido):

```tsx
{/* Herramientas de Office */}
<div className="space-y-3">
  <h2 className="text-xs font-medium uppercase tracking-wider text-muted-foreground">
    Herramientas de Office
  </h2>
  <div className="grid grid-cols-2 sm:grid-cols-3 gap-3">
    <Link href={`/workspace/${workspaceId}/docs`} ...>...</Link>
    <Link href={`/workspace/${workspaceId}/sheets`} ...>...</Link>
    <Link href={`/workspace/${workspaceId}/pdfs`} ...>...</Link>
  </div>
</div>
```

El `<div>` siguiente (lista de herramientas instaladas o empty state) permanece intacto.

- [ ] **Step 3: TypeScript check completo**

```powershell
cd C:\Users\priet\protools-hub\apps\web
npx tsc --noEmit 2>&1 | Select-Object -First 20
```

Expected: sin errores.

- [ ] **Step 4: Build**

```powershell
cd C:\Users\priet\protools-hub\apps\web
npx next build 2>&1 | Select-Object -Last 15
```

Expected: `✓ Compiled successfully`

- [ ] **Step 5: Commit**

```powershell
cd C:\Users\priet\protools-hub
git add "apps/web/src/app/(dashboard)/workspace/[workspaceId]/layout.tsx"
git add "apps/web/src/app/(dashboard)/workspace/[workspaceId]/tools/page.tsx"
git commit -m "feat: replace Docs/Sheets/PDFs sidebar items with Mi Office"
```

---

### Task 3: Deploy y verificación

**Files:** ninguno

- [ ] **Step 1: Deploy a producción**

```powershell
cd C:\Users\priet\protools-hub
npx vercel --prod --scope mitikus 2>&1 | Select-Object -Last 10
```

Expected: `"message": "Deployment ... ready."`

- [ ] **Step 2: Verificación manual en www.mitikus.com**

1. Abrir el workspace — el sidebar debe mostrar "Mi Office" en lugar de Docs + Hojas de cálculo + PDFs
2. Hacer clic en "Mi Office" → debe abrir la página hub con 3 cards
3. Hacer clic en cada card → debe navegar a `/docs`, `/sheets`, `/pdfs` correctamente
4. Ir a "Herramientas" → el bloque "Herramientas de Office" ya no debe aparecer
